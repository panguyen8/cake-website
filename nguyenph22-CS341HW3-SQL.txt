CREATE DATABASE CHEESECAKE;

USE CHEESECAKE;

CREATE TABLE ORDERS (
ORDERID int,
MONTH CHAR(3),
DAY INT,
QUANTITY int,
TOPPING ENUM("plain", "chocolate", "cherry"),
NOTES varchar(512)
);

DESCRIBE ORDERS;

SELECT * FROM ORDERS;

SELECT COUNT(TOPPING)
FROM ORDERS
WHERE TOPPING="plain" AND MONTH="FEB";

INSERT INTO ORDERS (ORDERID, MONTH, DAY, QUANTITY, TOPPING, NOTES) VALUES (888, "SEP", 28, 3, "cherry", "N/A");